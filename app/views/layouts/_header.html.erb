<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse"
          data-target="#header-collapseable-menu">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Coding Club', root_path, class: 'navbar-brand' %>
    </div>

    <div class="collapse navbar-collapse" id="header-collapseable-menu">
      <ul class="nav navbar-nav">
        <li><%= link_to 'Write', new_newsletter_path %></li>
        <li><%= link_to 'Find', newsletters_path %></li>
      </ul>

      <% if user_signed_in? %>
        <ul class="nav navbar-nav navbar-right">
          <li>
            <img class="img-circle" src="<%= current_user.profile_image_url %>"
            width="50" height="50" alt="<%= current_user.name %>’s profile image"
            style="cursor:pointer"/>
          </li>
          <li>
            <a href="javascript:void(0)">
              signed in as <%= current_user.name %>
            </a>
          </li>
          <li><%= link_to 'Logout', destroy_user_session_path %></li>
        </ul>
      <% end %>
    </div>
  </div>

  <% [:danger, :warning, :info, :success].each do |type| %>
    <%= content_tag(:div, flash[type], class: "alert alert-#{type}", style: 'margin:0;text-align:center') if flash[type] %>
  <% end %>
</nav>
